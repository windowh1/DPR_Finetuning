defaults:
  - datasets: retriever_default 

# Name of the question & answer resource
qa_dataset: nq_train

pyserini_index: 

# number of top(bottom)-k retrieval documents for each questions
retrieve_num: 100

# top or bottom-k retrieval
mode: top

# output path to write results to
out_path: 

# "regex" or "string"
match: string

validation_workers: 1

training_set:

retrieval_result_dpr: 


